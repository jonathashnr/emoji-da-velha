import React from "react";
import HeaderStyles from "./styles/Header.styled";

function Header() {
  return (
    <HeaderStyles>
      <h1 id="header">Emoji da Velha 👵</h1>
    </HeaderStyles>
  );
}

export default Header;
